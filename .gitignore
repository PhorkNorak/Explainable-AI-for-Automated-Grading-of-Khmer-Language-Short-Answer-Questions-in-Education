# Virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.py[cod]
*.pyo

# Model checkpoints & outputs
outputs/
*.pt
*.pth

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Large model artifacts — regenerable from training
*.safetensors
*.bin
*.joblib
**/lora_adapter/
**/checkpoints/

# Publishable model bundles (uploaded to HuggingFace, not tracked in git):
# weights exceed GitHub's 100 MB limit and belong on the Hub, not here.
publish/

# Auto-generated unsloth build cache
unsloth_compiled_cache/

# Bulky, regenerable per-cell predictions: keep ONLY the test split (it feeds the
# bootstrap CIs / paired tests). Train+val predictions are only used by exp02
# calibration at run time and are regenerated by re-running the experiments, so they
# are not tracked (they were ~250 MB across the grid).
**/predictions_train.csv
**/predictions_val.csv
